# 人物配置

普通用户不需要手工编辑配置。首次配置会从 `voices.example.json` 创建 `voices.local.json`。

模型、参考音频和头像路径均相对于项目根目录。`prompt_text` 必须与参考音频中实际说出的文字一致。三个首版角色的配置和模型文件由 Release 包提供。

发布包中的三个角色配置由 `voices.example.json` 提供。普通用户只需运行首次配置脚本，不需要手动填写模型路径。
