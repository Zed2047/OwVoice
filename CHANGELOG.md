# 更新日志

## v0.2.0

- 修复安装阶段错误地从 GPT-SoVITS 仓库查找 `fast_langdetect/lid.176.bin` 的问题。
- 完整语言识别模型改从 fastText 官方地址下载，并增加断点续传、重试、大小与 SHA256 校验；失败时自动使用轻量模型，不再阻断安装。
- 发布包内置签名并校验的 uv 0.12.12，支持现有 CPython 3.10.x x64（`>=3.10,<3.11`）创建项目环境；新安装推荐 3.10.10，3.10.21 已完成兼容性验证。
- 依赖改用 `uv.lock` 完整锁定；移除需要本地原生编译的 `jieba-fast` 与 `pyopenjtalk`，改用 `jieba` 和带 Windows wheel 的 `pyopenjtalk-plus`。
- CPU/GPU 均使用 CPU 版 ONNX Runtime，避免切换模式后与 `onnxruntime-gpu` 发生 DLL 冲突。
- 新增磁盘空间、目录权限、路径长度、VC++ 运行库、端口占用、进程占用和 NVIDIA 驱动版本预检，并统一生成安装日志。
- GPT-SoVITS 核心资源固定上游提交并增加关键文件校验；G2PW 与 NLTK 改为临时目录原子解压，避免中断后误判为安装成功。
- 应用更新改用 schema 3 校验清单，清单同时记录 CPython 3.10.x x64 兼容范围、依赖锁和资源锁身份；更新时优先复用兼容虚拟环境和相同 Torch，只同步变化项，并保留角色模型、预训练模型、头像、本地配置、输出及缓存。
- 发布包强制生成全新的空模型注册表；启动时会迁移旧绝对路径，导入时可自动登记已存在且完整的模型目录。
- 防止多个安装目录因共用本地端口而串用模型或训练记录；检测到其他 OwVoice 服务时会明确提示关闭。
- 模型导入统一以 `model.json` 中的 ID 为准；支持选择单个外部模型目录或包含多个模型的集合目录，并可原地登记 `data\\models` 中已有的模型而不重复复制权重。
- 发布脚本新增用户数据隔离硬检查，发现模型、训练记录、虚拟环境、缓存或音频残留时直接终止构建。
- 为 v0.1.2 用户提供一次性修复更新器，解决旧更新器首次跨版本更新不安全的问题。
- 修复 UpdateBridge 在部分 Windows PowerShell 控制台中重复显示中文字符的问题。
- 环境重建改为候选环境验证通过后原子切换；执行前展示 CPU/GPU、下载量与空间需求并要求明确同意，拒绝或失败时保留原环境。
- 模型注册表和训练任务改用原子 JSON 写入；注册表损坏时保留诊断副本，只在用户确认后根据合法模型目录重建。
- 加强模型元数据、权重类型、参考音频和推理参数校验；裸训练输出存在多个候选权重时要求用户明确选择。
- 训练流程增加磁盘空间门禁、精确产物追踪和临时模型包原子落盘，避免误选旧权重或留下半成品模型。
- 增加日志轮转、稳定错误编号、日志复制按钮和用户主动生成的脱敏诊断包；诊断包不会自动上传。
- 修复 Windows 中文路径/编码下英语词典读取异常。
- 修复切换、卸载模型与语音合成并发时的状态竞争，并增加模型状态检查。
- 修复提示词默认值缓存和 Qt 后台任务错误处理问题。
